# Credenciais — nunca commitar
claude/settings.local.json

# Segredos dos dotfiles. O ~/.zshenv.local mora no $HOME, não aqui — este padrão
# existe para o caso de uma cópia acidental cair dentro do repo (o .example, esse
# sim versionado, não casa com o padrão).
dotfiles/*.local

# Per-project runtime do Claude Code Desktop
.claude/

# Gerado pelo sistema
*.bak
